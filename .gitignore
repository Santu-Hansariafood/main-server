node_modules
.env


# mongoose.set("strictQuery", false);
# mongoose
#   .connect(process.env.MONGO_URL)
#   .then(() => {
#     console.log("Connected to DB");
#     app.listen(port, process.env.HOST, () => console.log(`Server running on ${process.env.HOST}:${port}`));
#   })
#   .catch((error) => {
#     console.log(error);
#   });